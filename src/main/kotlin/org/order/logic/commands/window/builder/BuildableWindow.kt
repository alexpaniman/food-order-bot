package org.order.logic.commands.window.builder

class BuildableWindow(private val marker: String, private val initialView: String) {
    private val views = mutableListOf<BuildableWindowView>()

    private fun build(): ExperimentalWindow {

    }
}